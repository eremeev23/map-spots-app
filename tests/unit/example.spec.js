import { shallowMount } from '@vue/test-utils';
import MapSidebar from '@/components/map/MapSidebar.vue';

describe('MapSidebar.vue', () => {
  let wrapper;

  beforeEach(() => {
    wrapper = shallowMount(MapSidebar);
  })
})
